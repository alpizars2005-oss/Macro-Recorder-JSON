# Macros

This folder is the default location for saved and opened macro files.

The application creates the folder automatically when it starts. Personal `.json` macro files stored here are ignored by Git so they are not uploaded to the repository by accident.

Keep `example_macro.json` in the project root as the public format example.
